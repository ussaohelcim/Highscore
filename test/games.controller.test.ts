import {GamesController} from "../src/games.controller"

test("adding a new game to db",()=>{
	let gc = new GamesController()
	// expect(gc.create({name:"name",id:2469})).tobe
})
// toBe("created a new game")